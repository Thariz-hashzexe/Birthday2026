Drop an mp3 file here named exactly:

  birthday-song.mp3

The player in index.html already points at assets/music/birthday-song.mp3,
so it will just start working once the file exists. Update the
displayed title in script.js under CONFIG.song.title.

Music never autoplays — the visitor has to press play.
